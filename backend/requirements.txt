fastapi>=0.111.0
uvicorn[standard]>=0.30.0
influxdb-client>=1.44.0
python-dotenv>=1.0.1
pydantic>=2.7.0
pydantic-settings>=2.3.0    # Required for config.py BaseSettings — separate from pydantic v2
httpx>=0.27.0
numpy>=1.24.0               # Use >= so pip can select a pre-built wheel (no C compiler needed)
scikit-learn>=1.3.0         # Available for future ML features; current predictions use numpy only
